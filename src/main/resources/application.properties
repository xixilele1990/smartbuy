spring.application.name=SmartBuy

# --- No-DB branch (lexyno-db) ---
# This branch intentionally does NOT persist to a database.

# --- ATTOM API ---
# Base URL only; endpoints are appended in code (e.g. /propertyapi/v1.0.0/...)
attom.base-url=${ATTOM_BASE_URL:https://api.gateway.attomdata.com}
# API key (hardcoded for now as requested)
attom.api-key=${ATTOM_API_KEY:940285cc500bb9f695235b40bccf698c}
